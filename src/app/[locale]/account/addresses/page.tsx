import SavedAddressesClient from '@/components/Account/SavedAddressesClient';

export default function SavedAddressesPage() {
    return (
        <main className="my-28">
            <SavedAddressesClient />
        </main>
    );
}
