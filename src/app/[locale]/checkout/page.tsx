import CheckoutClient from '@/components/Checkout/CheckoutClient';

export default function CheckoutPage() {
    return (
        <main className=" my-7 md:my-10">
            <CheckoutClient />
        </main>
    );
}
